// Matching makes both possible list shapes visible.
fn sum(items) {
    match items {
        [] => 0,
        [head, ..tail] => head + sum(tail),
    }
}

let numbers = [10, 20, 12];

println(numbers);
println(sum(numbers));
