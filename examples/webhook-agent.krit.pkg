schema = 1

[package]
name = "examples/webhook-agent"
version = "0.2.0"
edition = "2026"
entry = "webhook-agent.krit"
license = "Apache-2.0"

[capabilities]
config = ["agent.model"]
http = ["https://api.example.com"]
secrets = ["example-token"]
